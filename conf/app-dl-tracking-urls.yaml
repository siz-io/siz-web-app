# Tracking links depending on the `src` attibute in /get-the-app?src=xxx

default: # when src is empty, absent or unrecognized (not found below)
  ios: defaultIOS
  android: defaultANDROID
  forceStore: NO

tum:
  ios: tumIOS
  android: tumANDROID

fb:
  ios: fbIOS
  android: fbANDROID
